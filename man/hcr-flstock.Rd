% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FLStock-hcr.R
\docType{methods}
\name{hcr,FLStock,FLBRP-method}
\alias{hcr,FLStock,FLBRP-method}
\alias{hcr,biodyn-method}
\title{hcr}
\usage{
\S4method{hcr}{FLStock,FLBRP}(object, refs, hpar = hcrParam(ftar = 0.7 *
  refpts(refs)["msy", "harvest"], btrig = 0.8 * refpts(refs)["msy", "ssb"], fmin
  = 0.01 * refpts(refs)["msy", "harvest"], blim = 0.4 * refpts(refs)["msy",
  "ssb"]), yr = max(as.numeric(dimnames(catch(object))$year)) - 1, byr = yr,
  hyr = yr + 2:4, sr.residuals = FLQuant(1, dimnames = list(year = hyr)),
  tac = FALSE, tacMn = TRUE, bndF = NULL, bndTac = NULL, iaF = TRUE,
  iaTac = TRUE, maxF = 2, bEr = NULL, tEr = NULL, params = NULL,
  ref = stock.n(refs)[, 1], mdd = FALSE, matdd = FALSE, ...)
}
\arguments{
\item{object}{an object of class \code{FLStock} or}

\item{...}{other parameters, i.e.
params \code{FLPar} object with hockey stick HCR parameters, see hcrParam
yr numeric vector with years used to values in HCR
byr numeric vector with years used for bounds
hyr numeric vector with years to use in projection
tac \code{logical} should return value be TAC rather than F?
bndF \code{vector} with bounds (i.e.min and max values) on iter-annual variability on  F
bndTac \code{vector} with bounds (i.e. min and max values) on iter-annual variability on TAC}
}
\value{
\code{FLPar} object with value(s) for F or TAC if tac==TRUE
}
\description{
Harvest Control Rule, calculates F, or Total Allowable Catch (TAC) based on a hockey stock harvest control rule.
}
\examples{
\dontrun{
bd   =sim()

bd=window(bd,end=29)
for (i in seq(29,49,1))
bd=fwd(bd,harvest=hcr(bd,byr=i,yrs=i+1)$hvt)
}
}
\seealso{
\code{\link{bmsy}}, \code{\link{fmsy}}, \code{\link{fwd}} and \code{\link{hcrParam}}
}

